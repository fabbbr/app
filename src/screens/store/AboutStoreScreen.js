import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

export default function AboutStoreScreen({ id }) {
    return (
        <View>
            <Text>Store Screen about #{id}</Text>
        </View>
    )
}
